<?xml version="1.0" encoding="UTF-8"?>
<kml xmlns="http://www.opengis.net/kml/2.2">
   <Document>
      <name>Companies</name>
          <description>Nearby companies with interns</description>
          <% @kml.each do |placeholder| %>
            <%= render :partial => 'placemark', :locals => {:latitude => placeholder[0].latitude, :longitude => placeholder[0].longitude, :users => placeholder[1]} %>
          <% end %>
   </Document>
   </kml>
